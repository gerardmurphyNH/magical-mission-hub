# Copy to .env and fill in. The publishable/anon key is safe to expose in the client
# (security is enforced by Supabase Row Level Security), but keep it in env, not code.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
